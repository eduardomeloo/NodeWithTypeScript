name: Test com Jest
on:
  push:
    branches:
      - master

jobs:
  publish:
    name: Teste
    runs-on: ubuntu-latest
    steps:
      - actions/checkout@v2
      - actions/setup-node@v2
      with:
        node-version: 14.x
    - run: yarn
    - run: yarn test